[build]
  command = "npm install && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# SPA fallback (react-router)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
